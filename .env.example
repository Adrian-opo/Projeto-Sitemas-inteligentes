# Configurações do QR Reader Service
# Copie este arquivo para .env e ajuste conforme necessário

# Fonte da câmera (0 = webcam padrão, ou URL do IP Webcam)
QR_SOURCE=0

# Porta do servidor Flask do QR Reader (use 5001 no macOS para evitar conflito com AirPlay)
QR_PORT=5001

# URL do backend Django para enviar os pacotes
# Use 127.0.0.1:8001 para desenvolvimento local
# Use 127.0.0.1:8000 para Docker (ou o nome do serviço)
QR_BACKEND_URL=http://127.0.0.1:8001/api/arduino/pacote/

# Ativar cloudflare tunnel para acesso público (true/false)
QR_TUNNEL=false
